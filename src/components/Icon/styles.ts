import styled from "styled-components";

export const IconContainer = styled.div`
  width: 50%;
  padding: 5px;
  display: flex;
  cursor: pointer;
  justify-content: center;
  align-items: center;
  transition: 800ms;

  &:hover {
    transform: scale(1.3);
  }
`